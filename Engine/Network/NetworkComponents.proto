syntax = "proto2";
import 'nanopb.proto';

package usg.Components;

message HostManagedEntity
{
	option (nanopb_msgopt).has_onloaded = true;

	required bool bLocalSimCPUPlayerValue = 1 [default = true];
}